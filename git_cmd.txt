git config --global user.name 用户名;
git config --global user.email 邮箱;
git init;
git status;
git add 文件名;
git rm --cached 文件名;
git commit -m "注释日志" 文件名;
git reflog 查看历史版本; 
git reset --hard hashcode 切换到某个动作后的版本;
git log 查看详细版本;
